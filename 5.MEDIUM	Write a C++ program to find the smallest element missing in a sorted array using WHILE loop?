#include <iostream>
using namespace std;

int main() {
    int arr[] = {0, 1, 2, 3, 5, 6, 7}, n = sizeof(arr) / sizeof(arr[0]), i = 0;

    while (i < n && arr[i] == i) i++;

    cout << (i < n ? i : -1);

    return 0;
}
